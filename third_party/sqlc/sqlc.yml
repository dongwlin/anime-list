version: "2"
sql:
  - engine: "sqlite"
    queries: "./query"
    schema: "./schema"
    gen:
      go:
        package: "db"
        out: "../../internal/db"
        emit_interface: true
        emit_json_tags: true
        emit_empty_slices: true